# Standard new line ending
* text=auto
*.c text eol=crlf
*.h text eol=crlf
*.inl text eol=crlf

# Github's linguist thinks *.inl files are C++, even rename to h extension thinks it is Objective-C.
# Since it is not compiler dependent for .inl extension. Let's enforce it to C language.
*.inl linguist-language=C
